<Proc>
	<nombre>"bigbang"</nombre>
	<params>
	</params>
	<src>~crear procedimiento bigbang () {     declarar @resultado integer=0;       @resultado = genesis ();     si(@resultado == 3){         imprimir("las tablas se crearon correctamente y estn listas para ser pobladas.");         poblartablas();     }sino{         imprimir(morir());     } }~</src>
	</Proc>
<Proc>
	<nombre>"poblartablas"</nombre>
	<params>
	</params>
	<src>~crear procedimiento poblartablas () {            declarar @mimundo mundo;     @mimundo.ciudades = crearciudades();     @mimundo.paises = crearpaises ();     @mimundo.idiomas = crearidiomas ();      si(@mimundo.ciudades != 0) {         imprimir("ahora hay ");   imprimir(@mimundo.ciudades);   imprimir(" filas en la tabla city. :-)");     } sino {         imprimir(morir());     }      si(@mimundo.paises != 0){         imprimir("ahora hay ");   imprimir(@mimundo.paises);   imprimir(" filas en la tabla city. :-)");     } sino {         imprimir(morir());     }      si(@mimundo.idiomas != 0) {         imprimir("ahora hay ");   imprimir(@mimundo.idiomas);   imprimir(" filas en la tabla city. :-)");     } sino {         imprimir(morir());     } }~</src>
	</Proc>
<Proc>
	<nombre>"consultarmundo"</nombre>
	<params>
	<ENTERO>"tipo"</ENTERO>
	</params>
	<src>~crear procedimiento consultarmundo(integer @tipo) {   declarar @mimundo mundo;     @mimundo.ciudades = 58;     @mimundo.paises = 60;     @mimundo.idiomas = 63;      selecciona (@tipo) {         caso 100 :             si(@mimundo.paises != 0) {                 seleccionar * de country;             } sino {                 imprimir(morir());             }             detener;         caso 200 :             si(@mimundo.idiomas != 0 && @mimundo.paises != 0) {                 seleccionar countrylanguage.countrycode, country.name, countrylanguage.language, countrylanguage.percentage                 de countrylanguage, country                  donde countrylanguage.countrycode == country.code && countrylanguage.isofficial == verdadero;             } sino {                 imprimir(morir());             }             detener;         caso 300 :             si(@mimundo.idiomas != 0 && @mimundo.paises != 0) {                 seleccionar countrylanguage.language, countrylanguage.percentage                 de countrylanguage                  donde countrylanguage.countrycode == "gtm";             } sino {                 imprimir(morir());             }             detener;         caso 400 :             si(@mimundo.ciudades != 0 && @mimundo.paises != 0) {                             seleccionar id, name, district, population                  de city                  donde city.countrycode == "gtm";              } sino {                 imprimir(morir());             }             detener;         caso 500 :             si(@mimundo.idiomas != 0 && @mimundo.ciudades != 0 && @mimundo.paises != 0) {                           seleccionar country.population, country.code, country.name, city.name, countrylanguage.language                   de country, city, countrylanguage                 donde country.code == city.countrycode && country.code == countrylanguage.countrycode;                  declarar @temporal integer = 60;                 si( @temporal > 10) {                     declarar @a integer = 0;                     mientras( @a < 10 ) {                         imprimir("solo se necesitan 10 de " + @temporal + " y vamos por el " + @a);                         @a = @a + 1;                     }                 }             } sino {                 imprimir(morir());             }             detener;         caso 600 :             si(@mimundo.idiomas != 0 && @mimundo.ciudades != 0 && @mimundo.paises != 0) {                           seleccionar country.population, countrylanguage.language                 de country, city, countrylanguage                 donde country.code == city.countrycode && country.code == countrylanguage.countrycode;                  imprimir("solo tomar en cuenta los primeros 10");             } sino {                 imprimir(morir());             }                       detener;         caso 700 :             si(@mimundo.paises != 0) {                 si( 1 != 0 )  {                     actualizar tabla country (headofstate) valores ("jimmy morales") donde  country.code == "jpn";                 }                  si( 0 == 0) {                     imprimir("el pais no existe!!!!!");                 }                         seleccionar code, name, lifeexpectancy, headofstate                  de country ;                  declarar @temporal integer = 60;                 si( @temporal > 10) {                     para( declarar @a integer = 0; @a < 10; ++){                         imprimir("solo se necesitan 10 de " + @temporal + " y vamos por el " + @a);                     }                 }             } sino {                 imprimir(morir());             }                        detener;         caso 800 :             si(@mimundo.paises != 0) {                 imprimir("actualizando la tabla de paises...");                  si( 1 != 0) {                     actualizar tabla country (voz_popular) valores ("jimmy se va!!!!!") donde  country.code == "gtm";                 } sino {                     imprimir(morir() + ". qu pas, el pas si debe existir...");                 }                  seleccionar code, name, region, voz_popular                  de country                  donde country.region == "central america" && country.code != "mex";             } sino {                 imprimir(morir());             }             detener;         caso 900 :             si(@mimundo.paises != 0) {                 seleccionar code, name, region, voz_popular, population                  de country                  donde country.region == "central america" && country.code != "mex" && country.code != "blz" && country.code != "pan" && country.code != "cri" && country.code != "nic";                        declarar @otromundo mundo;                 @otromundo.nombre = "tierra";     @otromundo.paises = 0;     @otromundo.ciudades = 0;     @otromundo.idiomas = 0;                           imprimir("la " + @otromundo.nombre + " tiene:");     imprimir("paises " + @otromundo.paises);                 imprimir(@otromundo.paises);     imprimir("ciudades ");                 imprimir(@otromundo.ciudades);     imprimir("idiomas ");                 imprimir("idiomas " + @otromundo.idiomas);             } sino {                 imprimir(morir());             }                         detener;         caso 1000 :             si(@mimundo.paises != 0) {                 actualizar tabla country (continent) valores ("europe") donde country.code == "bra" || country.code == "gtm" || country.code == "nic" || country.code == "slv" || country.code == "hnd";                                 seleccionar code, name, lifeexpectancy                  de country                  donde country.continent == "europe" ;             } sino {                 imprimir(morir());             }              detener;         caso 1100 :             si(@mimundo.paises != 0) {                 declarar @cantidad integer;                 @cantidad = 14;                 imprimir("los pases donde el idioma espaol es el idioma oficial son: " + @cantidad);             } sino {                 imprimir(morir());             }             detener;                    caso 1200 :             si(@mimundo.idiomas != 0 && @mimundo.ciudades != 0 && @mimundo.paises != 0) {                 imprimir("eliminando columna voz_popular de la tabla country...");                 imprimir("23-09-2017 12:35:58");                  imprimir("iniciando la destruccin de africa...");                 imprimir("23-09-2017 12:35:58");                 imprimir("--------------------------------------------------------------------------");                  imprimir("eliminando los idiomas de los pases de africa con mayor poblacin...");                 imprimir("23-09-2017 12:35:58");                  si(56 == 56) {                                       imprimir("los idimoas se eliminaron correctamente.");                 } sino {                     imprimir(morir() + ". no se eliminaron los idiomas de africa que queramos.");                 }                  imprimir("eliminando los pases de africa con mayor poblacin...");                 imprimir("23-09-2017 12:35:58");                  si(53 == 53) {                                       imprimir("los pases se eliminaron correctamente.");                 } sino {                     imprimir(morir() + ". no se eliminaron los pases de africa que queramos.");                 }                  imprimir("eliminando las ciudades de los pases de africa con mayor poblacin...");                 imprimir("23-09-2017 12:35:58");                  si(51 == 51) {                                       imprimir("las ciudades se eliminaron correctamente.");                 } sino {                     imprimir(morir() + ". no se eliminaron las ciudades de los pases de africa que queramos.");                 }                  si(3 == 3) {                     seleccionar * de country donde country.continent == "africa";                 } sino {                     imprimir(morir() + ". algo sali mal al destruir africa, corregir o morir...");                 }                                             } sino {                 imprimir(morir());             }                       detener;         defecto :              imprimir("la opcin no es vlida... :-( ");     } }~</src>
	</Proc>
