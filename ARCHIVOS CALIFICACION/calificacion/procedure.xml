<Proc>
	<nombre>"insertar_primer_semestre"</nombre>
	<params>
	</params>
	<src>~crear procedimiento insertar_primer_semestre (){   insertar en tabla asignacion (772, 201403002, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (772, 201403003, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (772, 201403004, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (772, 201403006, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (772, 201403008, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (772, 201403009, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (772, 201403010, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (772, 201403011, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (772, 201403012, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (772, 201403013, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (772, 201403015, 17-01-2017 00:00:00, "c");    insertar en tabla asignacion (777, 201403002, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (777, 201403004, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (777, 201403006, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (777, 201403008, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (777, 201403007, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (777, 201403010, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (777, 201403014, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (777, 201403012, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (777, 201403013, 17-01-2017 00:00:00, "c");  insertar en tabla asignacion (777, 201403015, 17-01-2017 00:00:00, "c");  }~</src>
	</Proc>
<Proc>
	<nombre>"finalizar_primer_semestre"</nombre>
	<params>
	</params>
	<src>~crear procedimiento finalizar_primer_semestre (){   actualizar tabla asignacion (estado) valores ("a") donde carnet == 201403002 && (codigo_curso == 772 || codigo_curso == 777);  actualizar tabla asignacion (estado) valores ("a") donde carnet == 201403004 && (codigo_curso == 772 || codigo_curso == 777);  actualizar tabla asignacion (estado) valores ("a") donde carnet == 201403006 && (codigo_curso == 772 || codigo_curso == 777);  actualizar tabla asignacion (estado) valores ("a") donde carnet == 201403013 && (codigo_curso == 772 || codigo_curso == 777);  actualizar tabla asignacion (estado) valores ("a") donde carnet == 201403015 && (codigo_curso == 772 || codigo_curso == 777);  actualizar tabla asignacion (estado) valores ("a") donde carnet == 201403003 && (codigo_curso == 777);  actualizar tabla asignacion (estado) valores ("a") donde carnet == 201403010 && (codigo_curso == 772);   actualizar tabla asignacion (estado) valores ("r") donde estado == "c" && (codigo_curso == 772 || codigo_curso == 777); }~</src>
	</Proc>
<Proc>
	<nombre>"insertar_segundo_semestre"</nombre>
	<params>
	</params>
	<src>~crear procedimiento insertar_segundo_semestre (){  insertar en tabla asignacion (781, 201403002, 12-07-2017 00:00:00, "c");  insertar en tabla asignacion (781, 201403004, 12-07-2017 00:00:00, "c");  insertar en tabla asignacion (781, 201403006, 12-07-2017 00:00:00, "c");  insertar en tabla asignacion (781, 201403013, 12-07-2017 00:00:00, "c");  insertar en tabla asignacion (781, 201403015, 12-07-2017 00:00:00, "c"); }~</src>
	</Proc>
<Proc>
	<nombre>"finalizar_segundo_semestre"</nombre>
	<params>
	</params>
	<src>~crear procedimiento finalizar_segundo_semestre (){   actualizar tabla asignacion (estado) valores ("a") donde carnet == 201403002 && codigo_curso == 781;  actualizar tabla asignacion (estado) valores ("a") donde carnet == 201403004 && codigo_curso == 781;  actualizar tabla asignacion (estado) valores ("r") donde carnet != 201403002 && carnet != 201403004 && codigo_curso == 781;  }~</src>
	</Proc>
<Proc>
	<nombre>"insertar_tercer_semestre"</nombre>
	<params>
	</params>
	<src>~crear procedimiento insertar_tercer_semestre (){   insertar en tabla asignacion (281, 201403002, 17-01-2018 00:00:00, "c");  insertar en tabla asignacion (281, 201403004, 17-01-2018 00:00:00, "c");  }~</src>
	</Proc>
<Proc>
	<nombre>"consultas"</nombre>
	<params>
	</params>
	<src>~crear procedimiento consultas (){   seleccionar * de estudiante;   seleccionar * de curso;   seleccionar * de prerrequisito;   seleccionar * de asignacion;   seleccionar curso.nombre, prerrequisito.prerrequisito   de prerrequisito, curso   donde prerrequisito.codigo_curso == curso.codigo_curso;   seleccionar curso.nombre, estudiante.nombre, asignacion.estado   de curso, asignacion, estudiante   donde asignacion.codigo_curso == 281   && asignacion.carnet == estudiante.carnet   && asignacion.codigo_curso == curso.codigo_curso;   seleccionar curso.nombre, estudiante.nombre, asignacion.estado   de curso, asignacion, estudiante   donde asignacion.codigo_curso == 781   && asignacion.carnet == estudiante.carnet   && asignacion.codigo_curso == curso.codigo_curso;    seleccionar curso.nombre, estudiante.nombre, asignacion.estado   de curso, asignacion, estudiante   donde asignacion.carnet == estudiante.carnet   && asignacion.codigo_curso == curso.codigo_curso  && asignacion.fh_asignacion >= 12-07-2017 00:00:00;  }~</src>
	</Proc>
<Proc>
	<nombre>"ejecucion2"</nombre>
	<params>
	</params>
	<src>~crear procedimiento ejecucion2 (){  insertar_primer_semestre();  finalizar_primer_semestre();  insertar_segundo_semestre();  finalizar_segundo_semestre();  insertar_tercer_semestre();  consultas(); }~</src>
	</Proc>
<Proc>
	<nombre>"iniciar_archivo3"</nombre>
	<params>
	</params>
	<src>~crear procedimiento iniciar_archivo3(){  crear objeto estudiante  (   integer carnet,   text dpi,   text nombre,   text direccion  );   crear tabla bancodedatos(   text valor nulo,   integer tipo nulo,   integer orden nulo  );     crear tabla tmp_estudiante(   integer carnet nulo ,   integer dpi nulo,   text nombre nulo ,   text direccion nulo ,   datetime fh_nac nulo ,   datetime fh_creacion nulo   ); }~</src>
	</Proc>
<Proc>
	<nombre>"bancodatos"</nombre>
	<params>
	<ENTERO>"parametro_1"</ENTERO>
	<ENTERO>"parametro_2"</ENTERO>
	<ENTERO>"parametro_3"</ENTERO>
	</params>
	<src>~crear procedimiento bancodatos(integer @parametro_1, integer @parametro_2, integer @parametro_3) {   declarar @orden  integer=0;   si( @parametro_1 == 1)  {   @orden = @orden + 1;   insertar en tabla  bancodedatos ("ricardo arnulfo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("encarnacion",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("miguel  edmundo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("otto francisco",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("augusto adolfo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("wellington javier",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("karen ivonne",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("sergio estuardo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ingrid estela",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ingrid carolina",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("adelino",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("joshua emanuel",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("otto arturo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("olga amparo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("iris amabilia",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("milton eduardo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("maritza noemi",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("pablo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("patrocinio",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("esteban",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("jose fernel arnoldo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("maria del carmen",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("marvin geovany",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("eduardo misael",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("hector rolando",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("guillermo enrique",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("jose ros}o",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("cesar moises",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("miguel antonio",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("marga leticia",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("lissette oralia",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("erick enrique",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("hu rene",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ilce nohemi",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ethel corinna",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("alvaro humberto",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("eslin rodolfo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("leonel hipolito",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("mario leonid",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("mirna celeste amparo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("jorge fernando",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("carol alberto",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("erick arturo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("isai abimael",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("jessi(ca liliana",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("banner oswaldo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("jorge eduardo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("fernando enrique",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("cindy paola",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("die alejandro",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("br}a ninette",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ligia del pilar",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("hania del carmen",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ana cecilia",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("marvin alejandro",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("mildred ivonne",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("kissi(e maria",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("luis emilio",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("melsar",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("maria jose",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("javier rodri",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("amilcar estuardo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("francisco byron ismar",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("felix orlando",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("emilia",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("amilcar de jesus",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ruth angelica",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("mirla estefany",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("gustavo antonio",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("isauro",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("hada leslie",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("byron leopoldo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("karla julissa",1,@orden); @orden = @orden + 1;   }   si( @parametro_1 == 2)  {   insertar en tabla  bancodedatos ("ana lucia",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("manuel eduardo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("carlos efrain",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("jose",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("sergio oswaldo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("carlos rafael",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("alejandro",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("luis alberto",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("susana anabella",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("guadalupe",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("eder alejandro",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("jose mauricio",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("edgar mario",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("gemner oliverio",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ana victoria",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("si(lvia roxana",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("hu ricardo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("elvira del carmen",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ana lucrecia",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("edy gualter",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("pedro geovani",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("monica analy",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("jeannet si(omara",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("arnoldo benjamin",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("mauro",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("sergio francisco",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("jose david",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("edna amparo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("dougl napoleon",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("emperatriz luciana",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("sandra leticia",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("erick rodolfo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("edgar ulises",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("angela merici",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("cesar haroldo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("odilia ermelee",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("marvin bernal",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("alba dinorah",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("dulis roberto",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("walter fernando",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("claro de jesus",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("evelio de jesus",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("cesar alfredo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("mirsa elena",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ana isabel",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("rudy ignacio",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("monica ileana",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("braulio elizardo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("zully alejandra",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("kenneth max",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("herbert alejandro",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("catherin malena",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("anibal",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("walter emilio",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("andrea veronica",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("raul antonio",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("carolina isabel",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("armando nzalo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("gloria edelmira",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("victor vicente",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("karla johana paola",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("alfredo estuardo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("edilzar gilberto",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("byron rene",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("alma velinda",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("toribio valero",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("concha clarissa",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("edgar humberto",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("gl}iz alargys",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("si(lvia marina",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("jose domin",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("florencio pedro",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("edgardo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("jose carlos",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("irma lucrecia",1,@orden); @orden = @orden + 1;   }   si( @parametro_1 == 3)  {   insertar en tabla  bancodedatos ("elsa de los angeles",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("iraida del carmen",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ligia beatriz",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("sofia adderly",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("oscar orlando",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("aida eugenia",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("saul armando",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("jose roderico",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("elvis",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("allan fernando",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("alma sofia",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("kevin josue",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ruben",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("hector aparicio",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("hilda andrea",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("julia cecilia",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("sandra ninett",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ricardo arturo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("catalino",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("blanca irene",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("oscar jorge augusto",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("karina gabriela",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("angel mario",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("arcenio",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("jose maria",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("judith aracely",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("edgar alexander",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("victor gerardo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("lilian susana",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("erika julissa",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("sebtian",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("abner josue",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("miriam lilly",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("joel",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("romulo oswaldo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("luis gustavo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("boris edmundo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("rosa lorena",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("cesar",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("jairo gamaliel",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("byron alberto",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("bonsi(acio",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("oscar moises",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("hector adelso",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("mario randolfo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("gabriel angel",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("pedro javier",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("elsa carolina",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("maria esmeralda",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("jose otilio",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("marvin samuel",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("magdalena del consuelo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("hector amilcar",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("heidy yulisa",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("pedro roberto",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("paula josefina",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("gilberto alexis",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("victor hu",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("zoila esperanza",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("erwin antonio",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("mijail alexander",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("julio estuardo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("jose trinidad",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("mario leonel",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("edson roger",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("geovany alexander",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("arnold andre",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("lesbia mariela",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("alma lillian",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("guizlena marisabel",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("marlon danilo",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("julio alejandro",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("francisco alejandro",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("maria tatiana",1,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ana si(lvia",1,@orden); @orden = @orden + 1;   }     si( @parametro_2 == 1 )  {   insertar en tabla  bancodedatos ("leal buenafe",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("riv alvarado",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("toledo paredes",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("diaz maza",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("de paz quintana",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("prado carvajal",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("aguilar garcia",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("fernandez rueda",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("barrueto lopez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("linares herrera",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("nzalez alvarez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("lam duque",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("solorzano somoza",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ventura paniagua",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("rosales pacheco",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("martinez romero",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("mazaries garcia",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("diemecke nzalez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("lucero duarte",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("camar fernandez",2,@orden); @orden = @orden + 1;   }   si( @parametro_2 == 2)  {   insertar en tabla  bancodedatos ("fuentes lopez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("diaz aja",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("sazo manzo",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("lemus florian",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("meza duering",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("munhoz roman",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("hernandez zepeda",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("si(s si(s",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("davila letona",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("estrada leal",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("campos palma",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("boch alvarez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("minera ctillo",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("rossell argueta",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("garcia ramos",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("csi(uentes montoya",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("av}anho estrada",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("fajardo zamora",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("claudio archila",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("giordano villatoro",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("zunhiga carrillo",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("juarez perez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("sagtume acevedo",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("burs men}ez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("cancinos barrios",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ponciano mez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("lopez lopez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("pacheco cantoral",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("chinchilla perez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("sanchez de paz",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("mancilla giron",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ctillo pinzon",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("herrera bonilla",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ramirez hunter",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("aguilar ctillo",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("enriquez de leon",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("enriquez cruz",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("estrada solano",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("herrera valdez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("monroy mazaries",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("pineda aguirre",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("garcia arredondo",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("soto mendez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("osorio piox",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("alvarez najarro",2,@orden); @orden = @orden + 1;   }   si( @parametro_2 == 3)  {   insertar en tabla  bancodedatos ("montoya conde",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("alvarado mendez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ordonhez montes",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("florian hernandez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("polanco lopez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("orozco ",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("reyes cuev",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("bobadilla av}anho",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("argueta m}ez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("de la cerda paez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("barrios quinonez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("palencia prado",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("ibanhez paz",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("maldonado puac",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("estrada arevalo",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("estrada ctro",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("valdez flores",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("cuyan sazo",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("parada rodriguez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("bulum canti",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("cimiro alba",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("nzalez molina",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("saravia zelada",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("roca roca",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("salazar fuentes",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("villanueva guzman",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("mazaries alvarez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("caal nzalez",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("avila cabrera",2,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("rod bercian",2,@orden); @orden = @orden + 1;   }  si( @parametro_3 == 1 )  {   insertar en tabla  bancodedatos ("17 av. 7-19 zona 6 terranova villa nueva",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("2 avenida 4-14, zona 18 colonia san rafael i",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("km 16.5 ctra a elsalvador ca c-95 res. los diamantes sta catar pinula",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("9 avenida 11-36, zona 12",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("12 ave 17-85 c.k-6 zona 4 condado el naranjo cond. san agustin, mixco",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("2a. calle c 2-99 zona 7 villa nueva colin de monte maria sur",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("11 calle 1-41 zona 10 resi(denciales naciones unid 1, villa nueva",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("calzada san juan 31-81 zona 7",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("av. baja calsi(ornia #245 piso 11 dep. 1101 condesa del cuauhtemoc mexico df",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("4a. avenida 9-12, zona 1, san marcos",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("20 calle 20-23, zona 10",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("3a. calle 10-57, zona 3 nueva montserrat",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("manzana r, lote 8, san rafael el prado",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("santa cruz naranjo santa rosa",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("21 calle 7-15 zona 1 centro civico",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("5a. avenida 0-81, zona 1, colonia landivar",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("36 avenida a 15-43 zona 5, jardines de la uncion sur",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("a un costado de iglesi(a catolica barrio bethel, san luis peten",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("one new york plaza suite 4500 new york ny 10004-1562",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("08010 barcelona, espanha",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("4a. calle poniente final, local 23 mercado de artesania, antigua guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("37 calle 19-19  zona 12 guatemala guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("16 calle 21-85 zona 10, colonia concepcion",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("33 av. 23-52 zona 5 col. vivibien",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("39 calle 10-93 ca no.14 zona 11, el roble ii",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("3a. calle 6-23, zona 15",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("l. 29, fraccion e, finca companhia la cabanha km.34.5, col. los saucos, pacifico, palin escuintla",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("1300 ny.avenue n.w.whigton, d.c. 20577",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("villa los pinabetes km. 19-5 a san jose",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("3a. calle 10-38 zona 3 de mixco, colonia",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("34 avenida 5-11 zona 11 jardines de utatlan ii",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("26 calle 13-18, zona 13 ca 5 condominio panoramica",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("8ª calle 13-63  zona 8 mixco guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("7 times square, 45th. floor, ny 10036, usa",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("10 avenida 14-46 zona 10 guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("9a avenida 9-40, barrio el calvario zona 2",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("3a. av a ca a-21 resi(denciales el tabacal, zona 5 villa nueva, guatemala, guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("5a. av. 5-55 zona 14 europlaza",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("aldea santo domin los ocotes, san anto",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("5ta calle a 30-24 zona 7 colonia tikal 1",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("atescatempa, jutiapa",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("12 calle 4-15 zona 14",3,@orden); @orden = @orden + 1;   }   si( @parametro_3 == 2)  {   insertar en tabla  bancodedatos ("24 calle 1-05, zona 3",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("6a. calle 0-63, zona 1",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("1a. calle 10-70  zona 4 villa nueva guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("1a. avenida 47-73 monte maria i, zona 12, villa nueva, guatemala.",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("10 calle 2-84 z.9",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("32 avenida a 30-68 zona 5",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("4a. calle 0-74, zona 13, pamplona",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("8a. avenida 6-21 zona 14, colonia el campo",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("9a. calle 8-31, zona 1.",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("dianal seis 10-01  zona 10 guatemala guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("18 calle 6-72, zona 1",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("aldea el jocotillo, v.c.   zona 0 villa canales guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("25 avenida 17-25, zona 6 barrio san antonio",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("11 calle 3-06, zona 9",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("4a. calle a 24-44 zona 7, kaminal juyu i",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("6a. avenida 0-21 zona 1, jalapa",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("13 calle b 16-16 zona 7, vill de san",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("lote 51, manzana o, res. el quetzal san juan sacatepequez",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("11 avenida 2-03, zona 19 la florida, guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("colonia santa catalina cuilapa, santa rosa",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("3a  calle 6-11  zona 9 guatemala guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("7a. avenida 40-65, zona 5",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("5av. c 16-75 apto. a zona 5 col. 1 de julio mixco, guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("manzana j ca 38  z. 18, jardines del norte, guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("km. 52.8 carretera panamericana, el tejar, chimaltenan",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("4a. calle oriente, numero 2, zona 0, antigua guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("12 avenida 12-53 colegio de maestros zona 17",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("5 avenida a 2-57, bo. la democracia, j",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("4a. avenida 2-18, zona 1",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("20 avenida 19-02 colonia cipresales zona",3,@orden); @orden = @orden + 1;   }   si( @parametro_3 == 3)  {   insertar en tabla  bancodedatos ("2a. avenida 24-19, zona 3",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("manzana w, lote 8, condominio montecristo san jose pinula",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("7a. avenida 8-68, zona 4.",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("19 av. 10-16 apto. 3 zona 21 colonia ven",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("18 av. 14-43 zona 15 vista hermosa iii,",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("bo. san pedro, san pedro pinula, jalapa",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("lote 223, mz j, sector 11, prados de villa hermosa, san miguel petapa",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("21 ave b 0-79 ca 3 vista hermosa ii zona 15 cond villa florentina",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("km 17.5 al atlantico cs 6 mz b res. vall de sn jorge el fiscal palencia",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("13 calle 15-20 zona 11 guatemala, guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("2a. calle 15-57 boulevard vista hermosa,",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("14 avenida 0-08 zona 1 mixco, resi(d. raconal",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("manzana d lote 11, resi(denciales villa del sur",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("p.o. -box 31280",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("2a. avenida 4-66 zona 1, guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("0 avenida jardines de maribel lourdes ca 58 0-20 zona 16 guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("avenida 30 de junio barrio san miguel lote3-90 san jose escuintla",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("7a calle 62-60 pinares del norte, zona 18",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("41 avenida arrivillaga 23-05 zona 5, guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("12 avenida 10-25, zona 2, ciudad nueva, guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("2a calle 0-89 zona 4, mariaelena i, villa nueva",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("13 avenida a 19-61, zona 10, condominio vill marques",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("8a calle 3-31 zona 1, quetzaltenan",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("3a. calle 9-75 zona 18 res. atlantida guatemala, guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("19 avenida 2-71, zona 13 ribera del rio ii san miguel petapa",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("34 avenida a 23-56, zona 5, colonia vivibien",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("23 calle,  condado el naranjo 14-58  zona 4 mixco guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("calzada roosevelt centro comercial tikal futura, lcoales 236-237, fw 22-43, zona 11",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("6a. avenida 9-90 zona 3 colonia el gallito",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("1a. calle 16-28 zona 15, colonia el maes",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("33 av. 12-02 zona 5, colonia el eden, gu",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("eje 3 ca 28 sector torrelian, planes",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("4 calle 16-09 zona 1",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("blvd el naranjo ca d-9 zona 4 cond. san daniel, mixco",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("28 avenida 30-31, zona 5",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("avenida reforma 9-55, zona 10, edsi(icio reforma 10, piso 8, oficina 806",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("11 avenida 19-51 zona 1 guatemala, guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("colonia santia de los caballeros no 5a, antigua guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("8 avenida 6-26, zona 4 lom del mirador sumpan, sacatepequez",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("26 avenida, lote 26, finca el naranjo, zona 4, mixco",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("14 calle 0.20, zona 10",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("7 avenida 20-71, zona 1",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("12 calle 1-25, zona 10 edsi(icio geminis 10, torre sur",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("4 avenida 12-05, zona 1, barrio el carmen, solola",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("11 calle 1-24 zona 1 mazatenan suchitepequez",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("4a. avenida 6-80 a zona 2 , barrio lourdes, frente al estadio municipal, escuintla",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("4a avenida, san antonio 3-07, km. 17 a el salvador",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("2a. avenida 2-27, zona 4 , san jose poaquil, chimaltenan",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("7a. calle 1-67 zona 2 villa nueva san jose el placer",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("4 avenida 41-25, zona 12",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("carr. relleno km. 37.5, aldea el cerrito, lote 2, amatitlan",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("12 calle 1-25 zona 10 edsi(icio geminis oficina 1902, guatemala",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("6a. av. 4-81 zona 10",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("8a. avenida 5-81 zona 8 resi(denciales pradera de l flores condominio l magnoli ca 15 mixco",3,@orden); @orden = @orden + 1;   insertar en tabla  bancodedatos ("2a. av. 1-11 zona 4 santo domin xenaco",3,@orden); @orden = @orden + 1;  } }~</src>
	</Proc>
<Proc>
	<nombre>"primeringreso"</nombre>
	<params>
	<ENTERO>"inicio"</ENTERO>
	<ENTERO>"fin"</ENTERO>
	<ENTERO>"anho"</ENTERO>
	<ENTERO>"correlativo"</ENTERO>
	<ENTERO>"dpi_1"</ENTERO>
	<ENTERO>"dpi_correlativo"</ENTERO>
	<ENTERO>"dpi_ciudad"</ENTERO>
	</params>
	<src>~crear procedimiento primeringreso( integer @inicio, integer @fin, integer @anho, integer @correlativo,integer @dpi_1, integer @dpi_correlativo , integer @dpi_ciudad ){    declarar @x integer=0;  declarar @carnet integer=0;  declarar @alumno text="";  declarar @direccion text="";  declarar @dpi integer=0;    @carnet =  @correlativo;   @x = @inicio;  @carnet = (@anho * 100000) + @correlativo;    mientras ( @x < @fin ){      si (@x == 1){   @alumno="lazaro acunha orellana";    @direccion = "7a. avenida 20-98, zona 13 colonia aurora 2, apartamento c";}   si (@x == 2){@alumno="luis rolando najera rios";    @direccion = "km. 19.4 carretera a el salvador casa 84";}   si (@x == 3){@alumno="juan fernando mejia abrego";   @direccion = "17 calle 16-89 zona 10 jardines de oakland apartamento 14b";}   si (@x == 4){@alumno="jose miguel gonzalez martinez";   @direccion = "17 av. 18-89 bosques de miralvalle casa";}   si (@x == 5){@alumno="claudia lissette duarte gomez";    @direccion = "8 calle b 5-31 zona 8 valles de san cristobal mixco";}   si (@x == 6){@alumno="romulo medina diaz";    @direccion = "9 calle 11-13 casa d-3, z4 cond sta gabriela, condado naranjo, mixco";}   si (@x == 7){@alumno="carlos eduardo trujillo mendez";   @direccion = "20 avenida proyecto 4-10 22-59 zona 6 gu";}   si (@x == 8){@alumno="ysmael angel castanheda guerra";   @direccion = "avenida 25-56 san francisco i zona 6, mixco";}   si (@x == 9){@alumno="elena mercedes perez quan";    @direccion = "10 avenida 1-91, zona 2";}   si (@x == 10){@alumno="gilda xiomara gomez garcia";    @direccion = "10a. avenida 31-39, zona 5";}   si (@x == 11){@alumno="santos israel barrueto taque";   @direccion = "8a. avenida, lote 13, mz. h, residencial valle del norte, zona 18";}   si (@x == 12){@alumno="sergio vinicio arango makepeace";    @direccion = "4a. avenida 6-53 sector a-3, san cristobal i, zona de mixco";}   si (@x == 13){@alumno="pablo emilio rodriguez rivera";    @direccion = "2828 connecticut ave. nw apartament 704, washington dc, usa zipcode 20008";}   si (@x == 14){@alumno="robert anthony soto rivas";    @direccion = "via 5 1-36 apto. 3 zona 4 edificio cuatro36";}   si (@x == 15){@alumno="lazaro acunha orellana";    @direccion = "9a. avenida 39-97, zona 8";}   si (@x == 16){@alumno="luis rolando najera rios";    @direccion = "colonia 1323 apartamento 801, montevideo, uruguay";}   si (@x == 17){@alumno="juan fernando mejia abrego";    @direccion = "13 calle a 22-00 zona 7 kaminal juyu ii";}   si (@x == 18){@alumno="jose miguel gonzalez martinez";    @direccion = "18 av. 0-84 zona 6  colonia las brisas, mixco, guatemala.";}   si (@x == 18){@alumno="claudia lissette duarte gomez";    @direccion = "20 avenida 2-90, zona 13, rivera del rio, san miguel petapa";}      @dpi = @dpi_1 + ((@dpi_correlativo * 10 ^ 3)+ @x) + @dpi_ciudad;    insertar en tabla estudiante(@carnet,@dpi,@alumno,@direccion, 21-09-2017,21-09-2017 00:00:00);      @carnet = @carnet + 1;   @x = @x + 1;  }  seleccionar carnet , dpi de estudiante donde fh_creacion == 21-09-2017 && carnet > 201700000; }~</src>
	</Proc>
<Proc>
	<nombre>"copiar_estudiantes"</nombre>
	<params>
	<ENTERO>"nocopiar"</ENTERO>
	</params>
	<src>~crear procedimiento copiar_estudiantes(integer @nocopiar){     si(@nocopiar <=1){  insertar en tabla estudiante(@estudiante1.carnet,@estudiante1.dpi,@estudiante1.nombre,@estudiante1.direccion, 21-09-2017,21-09-2017 00:00:00);  }  si(@nocopiar <=2){  insertar en tabla estudiante(@estudiante2.carnet,@estudiante2.dpi,@estudiante2.nombre,@estudiante2.direccion, 21-09-2017,21-09-2017 00:00:00);  }  si(@nocopiar <=3){  insertar en tabla estudiante(@estudiante3.carnet,@estudiante3.dpi,@estudiante3.nombre,@estudiante3.direccion, 21-09-2017,21-09-2017 00:00:00);  }  si(@nocopiar <=4){  insertar en tabla estudiante(@estudiante4.carnet,@estudiante4.dpi,@estudiante4.nombre,@estudiante4.direccion, 21-09-2017,21-09-2017 00:00:00);  }  si(@nocopiar <=5){  insertar en tabla estudiante(@estudiante5.carnet,@estudiante5.dpi,@estudiante5.nombre,@estudiante5.direccion, 21-09-2017,21-09-2017 00:00:00);  }  si(@nocopiar <=6){  insertar en tabla estudiante(@estudiante6.carnet,@estudiante6.dpi,@estudiante6.nombre,@estudiante6.direccion, 21-09-2017,21-09-2017 00:00:00);  }  si(@nocopiar <=7){  insertar en tabla estudiante(@estudiante7.carnet,@estudiante7.dpi,@estudiante7.nombre,@estudiante7.direccion, 21-09-2017,21-09-2017 00:00:00);  }  si(@nocopiar <=8){  insertar en tabla estudiante(@estudiante8.carnet,@estudiante8.dpi,@estudiante8.nombre,@estudiante8.direccion, 21-09-2017,21-09-2017 00:00:00);  }  si(@nocopiar <=9){  insertar en tabla estudiante(@estudiante9.carnet,@estudiante9.dpi,@estudiante9.nombre,@estudiante9.direccion, 21-09-2017,21-09-2017 00:00:00);  }  si(@nocopiar <=10){  insertar en tabla estudiante(@estudiante10.carnet,@estudiante10.dpi,@estudiante10.nombre,@estudiante10.direccion, 21-09-2017,21-09-2017 00:00:00);  }  si(@nocopiar <=11){  insertar en tabla estudiante(@estudiante11.carnet,@estudiante11.dpi,@estudiante11.nombre,@estudiante11.direccion,21-09-2017,21-09-2017 00:00:00);  }  si(@nocopiar <=12){  insertar en tabla estudiante(@estudiante12.carnet,@estudiante12.dpi,@estudiante12.nombre,@estudiante12.direccion,21-09-2017,21-09-2017 00:00:00);  }  si(@nocopiar <=13){  insertar en tabla estudiante(@estudiante13.carnet,@estudiante13.dpi,@estudiante13.nombre,@estudiante13.direccion, 21-09-2017,21-09-2017 00:00:00);  }  si(@nocopiar <=14){  insertar en tabla estudiante(@estudiante14.carnet,@estudiante14.dpi,@estudiante14.nombre,@estudiante14.direccion, 21-09-2017,21-09-2017 00:00:00);  }  si(@nocopiar <=15){  insertar en tabla estudiante(@estudiante15.carnet,@estudiante15.dpi,@estudiante15.nombre,@estudiante15.direccion, 21-09-2017,21-09-2017 00:00:00);  }  seleccionar * de tmp_estudiante;   }~</src>
	</Proc>
<Proc>
	<nombre>"notas"</nombre>
	<params>
	<ENTERO>"nota_minima"</ENTERO>
	<ENTERO>"nota_maxima"</ENTERO>
	<ENTERO>"curso"</ENTERO>
	<ENTERO>"carnet"</ENTERO>
	<ENTERO>"cambio"</ENTERO>
	<ENTERO>"operacion"</ENTERO>
	<ENTERO>"final"</ENTERO>
	</params>
	<src>~crear procedimiento notas(integer @nota_minima, integer @nota_maxima, integer @curso, integer @carnet, integer @cambio, integer @operacion , integer @final){  declarar @carnet_nota integer = @carnet;  declarar @nota integer=0;  declarar @contador integer=0;  declarar @notatemp integer= @nota_minima;    actualizar tabla asignacion (nota) valores (0);    para ( declarar @iteracion integer = 0; @iteracion <= @final; ++){      imprimir("contador: " +  @contador);      si(@notatemp >= @nota_maxima || @notatemp <= 0){       si(@operacion == 1){     @nota = @nota - 1;    }    sino{     @nota = @nota + 1;    }    @notatemp = @nota;   }      si(@contador >= 0 && @contador <=3){    si(@operacion == 1){     @notatemp = @notatemp + (@cambio * @contador);    }    sino{     @notatemp = @notatemp - (@cambio * @contador);             }    }   sino{    @contador = 0;    si(@operacion == 1){     @nota = @nota - 1;}    sino{     @nota = @nota + 1;    }    @notatemp = @nota;     si(@operacion == 1){     @notatemp = @notatemp + (@cambio * @contador);}    sino{     @notatemp = @notatemp - (@cambio * @contador);    }   }   imprimir( "nota: "+ @notatemp );      actualizar tabla asignacion(nota) valores(@notatemp)   donde carnet == @carnet_nota && codigo_curso == @curso;      @contador = @contador + 1;   @carnet_nota = @carnet_nota + 1;     }    seleccionar * de asignacion donde codigo_curso == @curso; }~</src>
	</Proc>
<Proc>
	<nombre>"ejecutar_3"</nombre>
	<params>
	</params>
	<src>~crear procedimiento ejecutar_3(){    iniciar_archivo3();  imprimir("dpi : " + getdpiestudiante(201520868));  imprimir("dpi : " + getdpiestudiante(201520862));  imprimir("dpi : " + getdpiestudiante(201520855));  imprimir("dpi : " + getdpiestudiante(201520863));  imprimir("dpi : " + getdpiestudiante(201520859));  imprimir("dpi : " + getdpiestudiante(201520846));    imprimir("datos : " + getdatosestudiante(201520854));  imprimir("datos : " + getdatosestudiante(201520855));  imprimir("datos : " + getdatosestudiante(201520866));  imprimir("datos : " + getdatosestudiante(201520861));  imprimir("datos : " + getdatosestudiante(201520876));   imprimir("datos : " + getdatosestudiante(201520867));  imprimir("datos : " + getdatosestudiante(201520856));    bancodatos(1,2,1);  imprimir("nombres: " + 10);  imprimir("nombres: " + 10);  imprimir("nombres: " + 10);    bancodatos(3,1,3);  imprimir("nombres: " + 10);  imprimir("nombres: " + 10);  imprimir("nombres: " +10);    primeringreso(1, 12, 2017, 25000, 21230000, 4562, 12);      imprimir("direccion : " + fibonaccidireccion(201520854 , 201520856));  imprimir("direccion : " + fibonaccidireccion(201520854 , 201520854));   imprimir("direccion : " + fibonaccidireccion(201520854 , 201520857));   imprimir("direccion : " + fibonaccidireccion(201520854 , 201520861));   imprimir("direccion : " + fibonaccidireccion(201520854 , 201520866));     copiar_estudiantes(14);  notas(50, 80 , 772 , 201403002 , 5 , 1 , 10);   }~</src>
	</Proc>
